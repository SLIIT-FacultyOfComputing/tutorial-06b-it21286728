class Box {
    private:
       int length;
       int width;
       int height;
    public:
       // write prototypes of setters for length, width and height
       void setLength(int bxLength, int bxWidth, int bxHeight);
      // write prototypes of getters for length, width and height
       int getHeight();
       int getLength();
       int getWidth();
       int calcVolume();
};
